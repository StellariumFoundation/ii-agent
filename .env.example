# Your backend URL
# STATIC_FILE_BASE_URL (optional): If running the agent locally and want files in the workspace (e.g., images, static sites) to be accessible via HTTP URLs, set this to your local web server's base URL serving the 'workspace/' directory. Some tools use this to provide direct links to generated artifacts.
STATIC_FILE_BASE_URL=http://localhost:8000/

#Anthropic client key, for vertex, see README
ANTHROPIC_API_KEY=

# Search Provider API Keys
TAVILY_API_KEY=your_tavily_key # Primary search provider

# Optional: For alternative/enhanced search and crawl capabilities.
# SERPAPI_API_KEY (optional): Your API key for SerpAPI. Enables image search tool if set.
SERPAPI_API_KEY=
# FIRECRAWL_API_KEY (optional): Your API key for Firecrawl for web crawling.
FIRECRAWL_API_KEY=
# JINA_API_KEY (optional): Your API key for Jina AI, used for an alternative search/retrieval tool.
JINA_API_KEY=
